apiVersion: v1
kind: Service
metadata:
  name: opa-bundle
  namespace: baro-prod
  labels:
    app: opa-bundle
    component: cloud
spec:
  type: ClusterIP
  ports:
    - port: 8095
      targetPort: 8095
      protocol: TCP
      name: http
  selector:
    app: opa-bundle
