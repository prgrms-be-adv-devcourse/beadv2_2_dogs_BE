name: CI/CD Pipeline - Private EC2 (K8s Deployment)

# ==================================================
# Private EC2용 CI/CD Pipeline
# - 배포 대상: auth, buyer, seller, order, payment, support, settlement, ai, kafka, elasticsearch
# - 배포 방식: Kubernetes (k8s) - 애플리케이션 모듈
#            Docker Compose - Kafka, Elasticsearch (데이터 인프라)
# - Runner: private-ec2-runner (self-hosted)
# ==================================================

on:
  push:
    branches:
      - main-auth
      - main-buyer
      - main-seller
      - main-order
      - main-payment
      - main-support
      - main-settlement
      - main-ai
      - main-kafka
      - main-elasticsearch
  pull_request:
    branches:
      - main-auth
      - main-buyer
      - main-seller
      - main-order
      - main-payment
      - main-support
      - main-settlement
      - main-ai
      - main-kafka
      - main-elasticsearch
    types: [opened, synchronize, reopened]

# GITHUB_TOKEN에 패키지 쓰기 권한 부여
permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: do-develop-space
  IMAGE_VERSION: 0.1.0

jobs:
  # ==================================================
  # CI: Build, Test, Code Quality
  # ==================================================
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && 
       (startsWith(github.ref, 'refs/heads/main-auth') ||
        startsWith(github.ref, 'refs/heads/main-buyer') ||
        startsWith(github.ref, 'refs/heads/main-seller') ||
        startsWith(github.ref, 'refs/heads/main-order') ||
        startsWith(github.ref, 'refs/heads/main-payment') ||
        startsWith(github.ref, 'refs/heads/main-support') ||
        startsWith(github.ref, 'refs/heads/main-settlement') ||
        startsWith(github.ref, 'refs/heads/main-ai') ||
        startsWith(github.ref, 'refs/heads/main-kafka') ||
        startsWith(github.ref, 'refs/heads/main-elasticsearch'))) ||
      (github.event_name == 'pull_request' && 
       (startsWith(github.base_ref, 'main-auth') ||
        startsWith(github.base_ref, 'main-buyer') ||
        startsWith(github.base_ref, 'main-seller') ||
        startsWith(github.base_ref, 'main-order') ||
        startsWith(github.base_ref, 'main-payment') ||
        startsWith(github.base_ref, 'main-support') ||
        startsWith(github.base_ref, 'main-settlement') ||
        startsWith(github.base_ref, 'main-ai') ||
        startsWith(github.base_ref, 'main-kafka') ||
        startsWith(github.base_ref, 'main-elasticsearch')))
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Extract module name from branch
        id: module
        run: |
          if [[ "$GITHUB_REF" == refs/heads/main-* ]]; then
            MODULE=$(echo ${GITHUB_REF#refs/heads/main-} | tr '[:upper:]' '[:lower:]')
          else
            MODULE=""
          fi
          
          # 빌드할 모듈 결정 (Private EC2용: auth, buyer, seller, order, payment, support, settlement, ai만)
          # kafka와 elasticsearch는 빌드 불필요 (docker-compose로 배포)
          case "$MODULE" in
            auth) BUILD_MODULE=":baro-auth" ;;
            buyer) BUILD_MODULE=":baro-buyer" ;;
            seller) BUILD_MODULE=":baro-seller" ;;
            order) BUILD_MODULE=":baro-order" ;;
            payment) BUILD_MODULE=":baro-payment" ;;
            support) BUILD_MODULE=":baro-support" ;;
            settlement) BUILD_MODULE=":baro-settlement" ;;
            ai) BUILD_MODULE=":baro-ai" ;;
            kafka) BUILD_MODULE="" ;;  # kafka는 빌드 불필요
            elasticsearch) BUILD_MODULE="" ;;  # elasticsearch는 빌드 불필요
            *) BUILD_MODULE="" ;;
          esac
          
          echo "module=$MODULE" >> $GITHUB_OUTPUT
          echo "build_module=$BUILD_MODULE" >> $GITHUB_OUTPUT
          echo "Extracted module: $MODULE -> Build: $BUILD_MODULE"
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'
      
      - name: Cache Gradle build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Code formatting check (Spotless)
        continue-on-error: true
        run: |
          BUILD_MODULE="${{ steps.module.outputs.build_module }}"
          if [ -n "$BUILD_MODULE" ]; then
            ./gradlew ${BUILD_MODULE}:spotlessCheck
          else
            ./gradlew spotlessCheck
          fi
      
      - name: Lint check (Checkstyle)
        continue-on-error: true
        run: |
          BUILD_MODULE="${{ steps.module.outputs.build_module }}"
          if [ -n "$BUILD_MODULE" ]; then
            ./gradlew ${BUILD_MODULE}:checkstyleMain
          else
            ./gradlew checkstyleMain
          fi
      
      - name: Build with Gradle
        run: |
          BUILD_MODULE="${{ steps.module.outputs.build_module }}"
          if [ -n "$BUILD_MODULE" ]; then
            ./gradlew ${BUILD_MODULE}:build -x test
          else
            ./gradlew build -x test
          fi
      
      - name: Run tests
        continue-on-error: true
        run: |
          BUILD_MODULE="${{ steps.module.outputs.build_module }}"
          if [ -n "$BUILD_MODULE" ]; then
            ./gradlew ${BUILD_MODULE}:test
          else
            ./gradlew test
          fi
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: '**/build/test-results/test/'
          if-no-files-found: ignore
      
      - name: Determine artifact paths
        id: artifacts
        run: |
          MODULE="${{ steps.module.outputs.module }}"
          BUILD_MODULE="${{ steps.module.outputs.build_module }}"
          
          case "$MODULE" in
            auth) ARTIFACT_PATHS="baro-auth/build/libs/*.jar" ;;
            buyer) ARTIFACT_PATHS="baro-buyer/build/libs/*.jar" ;;
            seller) ARTIFACT_PATHS="baro-seller/build/libs/*.jar" ;;
            order) ARTIFACT_PATHS="baro-order/build/libs/*.jar" ;;
            payment) ARTIFACT_PATHS="baro-payment/build/libs/*.jar" ;;
            support) ARTIFACT_PATHS="baro-support/build/libs/*.jar" ;;
            settlement) ARTIFACT_PATHS="baro-settlement/build/libs/*.jar" ;;
            ai) ARTIFACT_PATHS="baro-ai/build/libs/*.jar" ;;
            elasticsearch) ARTIFACT_PATHS="" ;;  # elasticsearch는 빌드 불필요
            *) ARTIFACT_PATHS="" ;;
          esac
          
          echo "paths<<EOF" >> $GITHUB_OUTPUT
          echo "$ARTIFACT_PATHS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      
      - name: Upload build artifacts
        if: steps.artifacts.outputs.paths != ''
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            ${{ steps.artifacts.outputs.paths }}
          if-no-files-found: ignore

  # ==================================================
  # Docker: Build and Push Images
  # ==================================================
  determine-service:
    name: Determine Service to Build
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && 
       (startsWith(github.ref, 'refs/heads/main-auth') ||
        startsWith(github.ref, 'refs/heads/main-buyer') ||
        startsWith(github.ref, 'refs/heads/main-seller') ||
        startsWith(github.ref, 'refs/heads/main-order') ||
        startsWith(github.ref, 'refs/heads/main-payment') ||
        startsWith(github.ref, 'refs/heads/main-support') ||
        startsWith(github.ref, 'refs/heads/main-settlement') ||
        startsWith(github.ref, 'refs/heads/main-ai') ||
        startsWith(github.ref, 'refs/heads/main-kafka') ||
        startsWith(github.ref, 'refs/heads/main-elasticsearch'))) ||
      (github.event_name == 'pull_request' && 
       (startsWith(github.base_ref, 'main-auth') ||
        startsWith(github.base_ref, 'main-buyer') ||
        startsWith(github.base_ref, 'main-seller') ||
        startsWith(github.base_ref, 'main-order') ||
        startsWith(github.base_ref, 'main-payment') ||
        startsWith(github.base_ref, 'main-support') ||
        startsWith(github.base_ref, 'main-settlement') ||
        startsWith(github.base_ref, 'main-ai') ||
        startsWith(github.base_ref, 'main-kafka') ||
        startsWith(github.base_ref, 'main-elasticsearch')))
    outputs:
      service: ${{ steps.module.outputs.service }}
      module: ${{ steps.module.outputs.module }}
      image_tag: ${{ steps.module.outputs.image_tag }}
    steps:
      - name: Extract service name from branch
        id: module
        run: |
          if [[ "$GITHUB_REF" == refs/heads/main-* ]]; then
            MODULE=$(echo ${GITHUB_REF#refs/heads/main-} | tr '[:upper:]' '[:lower:]')
          else
            MODULE=""
          fi
          
          # 모듈명을 서비스명으로 변환 (Private EC2용만)
          # kafka와 elasticsearch는 docker-compose로 배포하므로 빌드 불필요
          case "$MODULE" in
            auth) SERVICE="baro-auth" ;;
            buyer) SERVICE="baro-buyer" ;;
            seller) SERVICE="baro-seller" ;;
            order) SERVICE="baro-order" ;;
            payment) SERVICE="baro-payment" ;;
            support) SERVICE="baro-support" ;;
            settlement) SERVICE="baro-settlement" ;;
            ai) SERVICE="baro-ai" ;;
            kafka) SERVICE="" ;;  # kafka는 빌드 불필요
            elasticsearch) SERVICE="" ;;  # elasticsearch는 빌드 불필요
            *) SERVICE="" ;;
          esac
          
          # 이미지 태그 결정
          if [[ "$GITHUB_REF" == refs/heads/main-* ]]; then
            IMAGE_TAG="latest"
          else
            # 기타 브랜치는 latest 사용 (fallback)
            IMAGE_TAG="latest"
          fi
          
          echo "module=$MODULE" >> $GITHUB_OUTPUT
          echo "service=$SERVICE" >> $GITHUB_OUTPUT
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "Extracted module: $MODULE -> service: $SERVICE with tag: $IMAGE_TAG"

  docker-build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [build-and-test, determine-service]
    timeout-minutes: 30
    if: |
      github.event_name == 'push' && 
      startsWith(github.ref, 'refs/heads/main-') &&
      needs.determine-service.outputs.service != '' &&
      needs.determine-service.outputs.module != 'kafka' &&
      needs.determine-service.outputs.module != 'elasticsearch'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'
      
      - name: Cache Gradle build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Download build artifacts
        continue-on-error: true  # 아티팩트가 없어도 계속 진행
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      
      - name: Build module if artifact not found
        run: |
          SERVICE="${{ needs.determine-service.outputs.service }}"
          case "$SERVICE" in
            baro-auth) MODULE_PATH="baro-auth" ;;
            baro-buyer) MODULE_PATH="baro-buyer" ;;
            baro-seller) MODULE_PATH="baro-seller" ;;
            baro-order) MODULE_PATH="baro-order" ;;
            baro-payment) MODULE_PATH="baro-payment" ;;
            baro-support) MODULE_PATH="baro-support" ;;
            baro-settlement) MODULE_PATH="baro-settlement" ;;
            baro-ai) MODULE_PATH="baro-ai" ;;
            *) MODULE_PATH="" ;;
          esac
          
          if [ -n "$MODULE_PATH" ]; then
            JAR_COUNT=$(find "$MODULE_PATH/build/libs" -name "*.jar" 2>/dev/null | wc -l)
            if [ "$JAR_COUNT" -eq 0 ]; then
              echo "JAR file not found, building module: $MODULE_PATH"
              GRADLE_MODULE=$(echo "$MODULE_PATH" | tr '/' ':')
              ./gradlew :${GRADLE_MODULE}:build -x test
            else
              echo "JAR file found in $MODULE_PATH/build/libs"
            fi
          fi
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:latest
            network=host
          buildkitd-flags: --allow-insecure-entitlement security.insecure --allow-insecure-entitlement network.host
          version: latest
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: do-develop-space
          password: ${{ secrets.GHCR_PAT }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ needs.determine-service.outputs.service }}
          tags: |
            type=raw,value=latest
            type=ref,event=branch
            type=sha,prefix={{branch}}-,format=short
            type=raw,value={{branch}}-{{date 'YYYYMMDD-HHmmss'}}
          flavor: |
            latest=true
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/${{ needs.determine-service.outputs.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ needs.determine-service.outputs.service }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ needs.determine-service.outputs.service }}:buildcache,mode=max
      
      - name: Make package public
        if: success()
        run: |
          PACKAGE_NAME="${{ needs.determine-service.outputs.service }}"
          echo "Making package ${PACKAGE_NAME} public..."
          
          curl -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/user/packages/container/${PACKAGE_NAME} \
            -d '{"visibility":"public"}' || echo "Failed to make package public (may already be public or require manual setup)"
          
          echo "✅ Package ${PACKAGE_NAME} visibility set to public"

  # ==================================================
  # CD: Deploy to Private EC2 (K8s)
  # ==================================================
  deploy:
    name: Deploy to Private EC2 (K8s)
    # GitHub-hosted runner에서 실행
    # kubeconfig를 통해 Kubernetes 클러스터에 원격 접근
    runs-on: ubuntu-latest
    needs: [build-and-test, determine-service, docker-build]
    timeout-minutes: 30  # 배포 타임아웃 설정
    if: |
      startsWith(github.ref, 'refs/heads/main-') &&
      needs.determine-service.outputs.module != '' &&
      always() && 
      (needs.determine-service.outputs.module == 'kafka' ||
       (needs.determine-service.outputs.module == 'elasticsearch' && vars.DEPLOY_ELASTICSEARCH != 'true') ||
       (needs.docker-build.result == 'success' || needs.docker-build.result == 'skipped'))
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Extract module name
        id: module
        run: |
          MODULE="${{ needs.determine-service.outputs.module }}"
          echo "name=$MODULE" >> $GITHUB_OUTPUT
          echo "branch=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT
          echo "🚀 Deploying module: $MODULE from branch: ${GITHUB_REF#refs/heads/}"

      - name: Setup SSH for deployment
        if: steps.module.outputs.name != 'kafka' && steps.module.outputs.name != 'elasticsearch'
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Verify kubectl connection (SSH)
        if: steps.module.outputs.name != 'kafka' && steps.module.outputs.name != 'elasticsearch'
        id: k3s-check
        env:
          EC2_HOST: ${{ secrets.PRIVATE_EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USERNAME || 'ubuntu' }}
        run: |
          # SSH를 통해 Private EC2에서 kubectl 연결 확인
          if ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get nodes &> /dev/null 2>&1 || sudo k3s kubectl get nodes &> /dev/null 2>&1"; then
            echo "k3s_available=true" >> $GITHUB_OUTPUT
            echo "kubectl_cmd=kubectl" >> $GITHUB_OUTPUT
            echo "✅ Kubernetes 클러스터에 연결되었습니다."
            echo "클러스터 노드 정보:"
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get nodes 2>&1 || sudo k3s kubectl get nodes 2>&1" || true
          else
            echo "k3s_available=false" >> $GITHUB_OUTPUT
            echo "❌ Kubernetes 클러스터에 연결할 수 없습니다."
            exit 1
          fi

      - name: Setup SSH for docker-compose deployment
        if: steps.module.outputs.name == 'kafka' || steps.module.outputs.name == 'elasticsearch'
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Deploy elasticsearch with docker-compose (SSH)
        if: |
          steps.module.outputs.name == 'elasticsearch' &&
          vars.DEPLOY_ELASTICSEARCH != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}
          EC2_HOST: ${{ secrets.PRIVATE_EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USERNAME || 'ubuntu' }}
        run: |
          echo "🚀 Elasticsearch docker-compose 배포 시작 (SSH를 통한 원격 배포)"
          
          # 필요한 파일들을 안전하게 복사 (기존 파일 보존)
          echo "📦 필요한 파일 복사 중 (기존 파일 보존)..."
          
          # docker-compose.elasticsearch.yml 복사 (기존 파일 백업)
          if [ -f docker-compose.elasticsearch.yml ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "if [ -f /home/ubuntu/apps/BE/docker-compose.elasticsearch.yml ]; then cp /home/ubuntu/apps/BE/docker-compose.elasticsearch.yml /home/ubuntu/apps/BE/docker-compose.elasticsearch.yml.bak.\$(date +%Y%m%d_%H%M%S) 2>/dev/null || true; fi"
            scp -o StrictHostKeyChecking=no docker-compose.elasticsearch.yml ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/ || {
              echo "⚠️  docker-compose.elasticsearch.yml 복사 실패, 기존 파일 사용"
            }
          fi
          
          # deploy-module.sh 스크립트 복사 (scripts 디렉토리 보존, 기존 파일 백업)
          if [ -f scripts/deploy-module.sh ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/BE/scripts && if [ -f /home/ubuntu/apps/BE/scripts/deploy-module.sh ]; then cp /home/ubuntu/apps/BE/scripts/deploy-module.sh /home/ubuntu/apps/BE/scripts/deploy-module.sh.bak.\$(date +%Y%m%d_%H%M%S) 2>/dev/null || true; fi"
            scp -o StrictHostKeyChecking=no scripts/deploy-module.sh ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/scripts/ || {
              echo "⚠️  deploy-module.sh 복사 실패, 기존 파일 사용"
            }
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "chmod +x /home/ubuntu/apps/BE/scripts/deploy-module.sh"
          fi
          
          # Elasticsearch 빌드 컨텍스트 복사 (기존 파일 보존)
          if [ -d docker/baro-es ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/BE/docker/baro-es && if [ -d /home/ubuntu/apps/BE/docker/baro-es ] && [ \"\$(ls -A /home/ubuntu/apps/BE/docker/baro-es 2>/dev/null)\" ]; then mkdir -p /home/ubuntu/apps/BE/docker/baro-es.backup.\$(date +%Y%m%d_%H%M%S) && cp -r /home/ubuntu/apps/BE/docker/baro-es/* /home/ubuntu/apps/BE/docker/baro-es.backup.\$(date +%Y%m%d_%H%M%S)/ 2>/dev/null || true; fi"
            # rsync를 사용하여 기존 파일 보존하며 복사 (없는 파일만 추가)
            if command -v rsync &> /dev/null; then
              rsync -av --ignore-existing docker/baro-es/ ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/docker/baro-es/ 2>/dev/null || {
                echo "⚠️  rsync 실패, scp로 재시도..."
                scp -o StrictHostKeyChecking=no -r docker/baro-es/* ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/docker/baro-es/ 2>/dev/null || {
                  echo "⚠️  Elasticsearch 빌드 컨텍스트 복사 실패 (계속 진행)"
                }
              }
            else
              scp -o StrictHostKeyChecking=no -r docker/baro-es/* ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/docker/baro-es/ 2>/dev/null || {
                echo "⚠️  Elasticsearch 빌드 컨텍스트 복사 실패 (계속 진행)"
              }
            fi
          fi
          
          # SSH를 통해 배포 실행
          ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "cd /home/ubuntu/apps/BE || mkdir -p /home/ubuntu/apps/BE && cd /home/ubuntu/apps/BE && if [ ! -f docker-compose.elasticsearch.yml ]; then echo '❌ docker-compose.elasticsearch.yml 파일을 찾을 수 없습니다.' && exit 1; fi && if [ -f scripts/deploy-module.sh ]; then GITHUB_TOKEN='${GITHUB_TOKEN}' bash scripts/deploy-module.sh elasticsearch; else echo '❌ scripts/deploy-module.sh를 찾을 수 없습니다.' && exit 1; fi"

      - name: Deploy kafka with docker-compose (SSH)
        if: steps.module.outputs.name == 'kafka'
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}
          EC2_HOST: ${{ secrets.PRIVATE_EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USERNAME || 'ubuntu' }}
        run: |
          echo "🚀 Kafka docker-compose 배포 시작 (SSH를 통한 원격 배포)"
          
          # 필요한 파일들을 안전하게 복사 (기존 파일 보존)
          echo "📦 필요한 파일 복사 중 (기존 파일 보존)..."
          
          # docker-compose.kafka.yml 복사 (기존 파일 백업)
          if [ -f docker-compose.kafka.yml ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "if [ -f /home/ubuntu/apps/BE/docker-compose.kafka.yml ]; then cp /home/ubuntu/apps/BE/docker-compose.kafka.yml /home/ubuntu/apps/BE/docker-compose.kafka.yml.bak.\$(date +%Y%m%d_%H%M%S) 2>/dev/null || true; fi"
            scp -o StrictHostKeyChecking=no docker-compose.kafka.yml ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/ || {
              echo "⚠️  docker-compose.kafka.yml 복사 실패, 기존 파일 사용"
            }
          fi
          
          # deploy-module.sh 스크립트 복사 (scripts 디렉토리 보존, 기존 파일 백업)
          if [ -f scripts/deploy-module.sh ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/BE/scripts && if [ -f /home/ubuntu/apps/BE/scripts/deploy-module.sh ]; then cp /home/ubuntu/apps/BE/scripts/deploy-module.sh /home/ubuntu/apps/BE/scripts/deploy-module.sh.bak.\$(date +%Y%m%d_%H%M%S) 2>/dev/null || true; fi"
            scp -o StrictHostKeyChecking=no scripts/deploy-module.sh ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/scripts/ || {
              echo "⚠️  deploy-module.sh 복사 실패, 기존 파일 사용"
            }
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "chmod +x /home/ubuntu/apps/BE/scripts/deploy-module.sh"
          fi
          
          # SSH를 통해 배포 실행
          ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "cd /home/ubuntu/apps/BE || mkdir -p /home/ubuntu/apps/BE && cd /home/ubuntu/apps/BE && if [ ! -f docker-compose.kafka.yml ]; then echo '❌ docker-compose.kafka.yml 파일을 찾을 수 없습니다.' && exit 1; fi && if [ -f scripts/deploy-module.sh ]; then GITHUB_TOKEN='${GITHUB_TOKEN}' bash scripts/deploy-module.sh kafka; else echo '❌ scripts/deploy-module.sh를 찾을 수 없습니다.' && exit 1; fi"

      - name: Deploy to k8s (SSH)
        if: steps.k3s-check.outputs.k3s_available == 'true' && steps.module.outputs.name != 'kafka' && steps.module.outputs.name != 'elasticsearch'
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}
          IMAGE_TAG: ${{ needs.determine-service.outputs.image_tag }}
          MODULE_NAME: ${{ steps.module.outputs.name }}
          EC2_HOST: ${{ secrets.PRIVATE_EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USERNAME || 'ubuntu' }}
          DATA_EC2_IP: ${{ secrets.DATA_EC2_IP || '' }}  # Data EC2 Private IP (필수)
          DEPLOY_KAFKA: ${{ vars.DEPLOY_KAFKA || 'false' }}  # Kafka 배포 여부
          DEPLOY_ELASTICSEARCH: ${{ vars.DEPLOY_ELASTICSEARCH || 'false' }}  # Elasticsearch 배포 여부
        run: |
          echo "🚀 k8s 배포 시작 (SSH를 통한 원격 배포): ${MODULE_NAME} (이미지 태그: ${IMAGE_TAG})"
          
          # 필요한 파일들을 안전하게 복사 (기존 파일 보존)
          echo "📦 필요한 파일 복사 중 (기존 파일 보존)..."
          
          # k8s 디렉토리 복사 (기존 파일 보존)
          if [ -d k8s ]; then
            # 기존 k8s 디렉토리의 추가 파일들 백업 (scripts, README.md, .env 등)
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/k8s && if [ -d /home/ubuntu/apps/k8s/scripts ] && [ \"\$(ls -A /home/ubuntu/apps/k8s/scripts 2>/dev/null)\" ]; then mkdir -p /home/ubuntu/apps/k8s.backup.scripts.\$(date +%Y%m%d_%H%M%S) && cp -r /home/ubuntu/apps/k8s/scripts /home/ubuntu/apps/k8s.backup.scripts.\$(date +%Y%m%d_%H%M%S)/ 2>/dev/null || true; fi"
            # k8s 디렉토리 복사 (최신 파일로 강제 덮어쓰기)
            echo "📦 k8s 디렉토리 복사 중 (최신 파일로 강제 덮어쓰기)..."
            # apps/, cloud/, redis/ 등 주요 디렉토리의 YAML 파일들을 먼저 삭제
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "
              cd /home/ubuntu/apps/k8s 2>/dev/null || true
              find apps cloud redis -type f \\( -name 'deployment.yaml' -o -name 'service.yaml' -o -name 'kustomization.yaml' -o -name 'daemonset.yaml' \\) -delete 2>/dev/null || true
              echo '🗑️  기존 YAML 파일 삭제 완료'
            "
            # rsync로 복사 (--times 옵션으로 타임스탬프도 업데이트, --no-perms로 권한 문제 방지)
            if command -v rsync &> /dev/null; then
              rsync -av --times --no-perms k8s/ ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/k8s/ || {
                echo "⚠️  rsync 실패, scp로 재시도..."
                # scp로 강제 복사
                ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/k8s"
                scp -o StrictHostKeyChecking=no -r k8s/* ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/k8s/ || {
                  echo "❌ k8s 디렉토리 복사 실패"
                  exit 1
                }
              }
            else
              # scp 사용
              ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/k8s"
              scp -o StrictHostKeyChecking=no -r k8s/* ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/k8s/ || {
                echo "❌ k8s 디렉토리 복사 실패"
                exit 1
              }
            fi
            # 복사 후 파일 날짜 확인
            echo "📅 복사된 파일 날짜 확인:"
            # 배포 중인 모듈의 파일 확인
            if [ -n "${MODULE_NAME}" ]; then
              K8S_APP_NAME="baro-${MODULE_NAME}"
              echo "📦 배포 중인 모듈 파일 확인: ${K8S_APP_NAME}"
              ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "ls -lh /home/ubuntu/apps/k8s/apps/${K8S_APP_NAME}/*.yaml 2>/dev/null || echo '⚠️  ${K8S_APP_NAME} 파일을 찾을 수 없습니다'"
            fi
            # 모든 apps 디렉토리 파일 목록 확인
            echo "📦 모든 apps 모듈 파일 목록:"
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "find /home/ubuntu/apps/k8s/apps -name '*.yaml' -type f 2>/dev/null | head -20 || echo '파일을 찾을 수 없습니다'"
            echo "✅ k8s 디렉토리 복사 완료 (최신 파일로 업데이트됨)"
            # 백업한 scripts 디렉토리 복원
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "BACKUP_SCRIPTS=\$(ls -td /home/ubuntu/apps/k8s.backup.scripts.* 2>/dev/null | head -1) && if [ -n \"\$BACKUP_SCRIPTS\" ] && [ -d \"\$BACKUP_SCRIPTS/scripts\" ] && [ ! -d /home/ubuntu/apps/k8s/scripts ] || [ -z \"\$(ls -A /home/ubuntu/apps/k8s/scripts 2>/dev/null)\" ]; then mkdir -p /home/ubuntu/apps/k8s/scripts && cp -r \"\$BACKUP_SCRIPTS/scripts\"/* /home/ubuntu/apps/k8s/scripts/ 2>/dev/null || true && echo '✅ scripts 디렉토리 복원 완료'; fi"
          fi
          
          # deploy-k8s.sh 스크립트 복사 (기존 파일 백업)
          # settlement도 이제 deploy-k8s.sh 사용 (Deployment + CronJob)
          DEPLOY_SCRIPT="scripts/deploy-k8s.sh"
          
          if [ -f "$DEPLOY_SCRIPT" ]; then
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "mkdir -p /home/ubuntu/apps/BE/scripts && if [ -f /home/ubuntu/apps/BE/$DEPLOY_SCRIPT ]; then cp /home/ubuntu/apps/BE/$DEPLOY_SCRIPT /home/ubuntu/apps/BE/$DEPLOY_SCRIPT.bak.\$(date +%Y%m%d_%H%M%S) 2>/dev/null || true; fi"
            scp -o StrictHostKeyChecking=no "$DEPLOY_SCRIPT" ${EC2_USER}@${EC2_HOST}:/home/ubuntu/apps/BE/"$DEPLOY_SCRIPT" || {
              echo "⚠️  $DEPLOY_SCRIPT 복사 실패, 기존 파일 사용"
            }
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "chmod +x /home/ubuntu/apps/BE/$DEPLOY_SCRIPT"
          else
            echo "❌ 배포 스크립트를 찾을 수 없습니다: $DEPLOY_SCRIPT"
            exit 1
          fi
          
          # SSH를 통해 배포 실행 (Private EC2에서 직접 kubectl 사용)
          ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "cd /home/ubuntu/apps/BE && if [ ! -d k8s ]; then echo '❌ k8s 디렉토리를 찾을 수 없습니다.' && exit 1; fi && if [ ! -f $DEPLOY_SCRIPT ]; then echo '❌ $DEPLOY_SCRIPT를 찾을 수 없습니다.' && exit 1; fi && export DATA_EC2_IP='${DATA_EC2_IP}' DEPLOY_KAFKA='${DEPLOY_KAFKA}' DEPLOY_ELASTICSEARCH='${DEPLOY_ELASTICSEARCH}' && GITHUB_TOKEN='${GITHUB_TOKEN}' bash $DEPLOY_SCRIPT ${MODULE_NAME} ${IMAGE_TAG}" || {
            echo "❌ 배포 스크립트 실행 실패"
            # 원격에서 Pod 상태 확인
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || sudo k3s kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || true"
            exit 1
          }
          
          echo ""
          echo "✅ 배포 완료: ${MODULE_NAME}"
          
          # 배포 후 검증 (원격에서 확인)
          echo ""
          echo "🔍 배포 검증 중..."
          sleep 5  # Pod 시작 대기
          
          # Pod 상태 확인
          echo "📊 Pod 상태:"
          ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || sudo k3s kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || true"
          
          # Pod가 Ready 상태인지 확인
          if ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl wait --for=condition=ready pod -l app=baro-${MODULE_NAME} -n baro-prod --timeout=120s 2>/dev/null || sudo k3s kubectl wait --for=condition=ready pod -l app=baro-${MODULE_NAME} -n baro-prod --timeout=120s 2>/dev/null"; then
            echo "✅ Pod가 Ready 상태입니다."
          else
            echo "⚠️  Pod가 Ready 상태가 되지 않았습니다."
            echo "📋 Pod 상세 정보:"
            ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} -o wide 2>&1 || sudo k3s kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} -o wide 2>&1 || true"
            
            # 최신 Pod 로그 출력
            LATEST_POD=$(ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} --sort-by=.metadata.creationTimestamp -o jsonpath='{.items[-1].metadata.name}' 2>/dev/null || sudo k3s kubectl get pods -n baro-prod -l app=baro-${MODULE_NAME} --sort-by=.metadata.creationTimestamp -o jsonpath='{.items[-1].metadata.name}' 2>/dev/null || echo \"\"")
            if [ -n "$LATEST_POD" ]; then
              echo ""
              echo "📋 최신 Pod 로그 ($LATEST_POD):"
              ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl logs -n baro-prod \"$LATEST_POD\" --tail=50 2>&1 || sudo k3s kubectl logs -n baro-prod \"$LATEST_POD\" --tail=50 2>&1 || true"
              
              echo ""
              echo "📊 Pod 이벤트:"
              ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl describe pod -n baro-prod \"$LATEST_POD\" 2>&1 | grep -A 20 'Events:' || sudo k3s kubectl describe pod -n baro-prod \"$LATEST_POD\" 2>&1 | grep -A 20 'Events:' || true"
            fi
          fi
          
          # Service 확인 (원격에서 확인)
          echo ""
          echo "📊 Service 상태:"
          ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} "kubectl get svc -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || sudo k3s kubectl get svc -n baro-prod -l app=baro-${MODULE_NAME} 2>&1 || true"


  # ==================================================
  # Cleanup: 오래된 이미지 정리
  # ==================================================
  cleanup:
    name: Cleanup Old Images
    runs-on: ubuntu-latest
    needs: [deploy, determine-service]
    if: |
      success() &&
      needs.determine-service.outputs.service != ''
    permissions:
      contents: read
      packages: write

    steps:
      - name: Cleanup container images by tag policy
        env:
          SERVICE: ${{ needs.determine-service.outputs.service }}
          OWNER: ${{ github.repository_owner }}
          GH_TOKEN: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}
          IMAGE_VERSION: ${{ env.IMAGE_VERSION }}
        run: |
          if [ -z "$SERVICE" ]; then
            echo "No service to clean up. Skipping."
            exit 0
          fi

          if [ -z "$GH_TOKEN" ]; then
            echo "❌ GH_TOKEN is not set."
            exit 1
          fi

          echo "🧹 Cleaning up GHCR images for service: $SERVICE (owner: $OWNER)"
          echo "  - Keep tags: latest, $IMAGE_VERSION"

          sudo apt-get update -y >/dev/null 2>&1 || true
          sudo apt-get install -y jq >/dev/null 2>&1 || true

          API_ROOT="https://api.github.com"

          VERSIONS_JSON=$(curl -sS \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "${API_ROOT}/users/${OWNER}/packages/container/${SERVICE}/versions?per_page=100")

          if echo "$VERSIONS_JSON" | jq -e 'type=="array"' >/dev/null 2>&1; then
            :
          else
            echo "No versions found or failed to fetch versions."
            exit 0
          fi

          KEEP_TAG_1="latest"
          KEEP_TAG_2="$IMAGE_VERSION"

          echo "$VERSIONS_JSON" | jq -c '.[]' | while read -r row; do
            ID=$(echo "$row" | jq -r '.id')
            CREATED_AT=$(echo "$row" | jq -r '.created_at')
            TAGS=$(echo "$row" | jq -r '.metadata.container.tags | join(",")')

            if [ "$TAGS" = "null" ] || [ -z "$TAGS" ]; then
              SHOULD_DELETE=1
            else
              SHOULD_DELETE=1
              if echo "$TAGS" | grep -q "$KEEP_TAG_1"; then
                SHOULD_DELETE=0
              fi
              if [ "$SHOULD_DELETE" -eq 1 ] && [ -n "$KEEP_TAG_2" ] && echo "$TAGS" | grep -q "$KEEP_TAG_2"; then
                SHOULD_DELETE=0
              fi
            fi

            echo "Version ID=${ID}, created_at=${CREATED_AT}, tags=[${TAGS}]"

            if [ "$SHOULD_DELETE" -eq 1 ]; then
              echo "  -> Deleting this version"
              curl -sS -X DELETE \
                -H "Authorization: Bearer $GH_TOKEN" \
                -H "Accept: application/vnd.github+json" \
                "${API_ROOT}/user/packages/container/${SERVICE}/versions/${ID}" || echo "  ⚠️ Failed to delete version ${ID}"
            else
              echo "  -> Keeping this version"
            fi
          done

